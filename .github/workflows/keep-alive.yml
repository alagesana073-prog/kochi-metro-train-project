name: Keep Render Server Alive

on:
  schedule:
    # Run every 10 minutes to prevent Render free tier from sleeping (it sleeps after 15 mins)
    - cron: "*/10 * * * *"
  workflow_dispatch: # Allows manual triggering from GitHub UI

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API Health Endpoint
        run: |
          echo "Pinging backend server to keep it awake..."
          curl -s -f https://backend-j57u.onrender.com/health || echo "Failed to ping, but workflow continues."
